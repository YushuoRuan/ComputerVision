function outlierMap = outliers(DLR, DRL, TLR)

[r,c] = size(DLR);

outlierMap = zeros(r,c);

for i = r
    for j = c
        if abs(DLR(i,j) - DRL(i-,j)) > TLR
            outlierMap(i,j) = 1;
        end
    end
end


end